name = "developer"
description = "Python/Jinja2/YAML implementation and testing for Abhaile."
nickname_candidates = ["Developer", "Builder", "Implementer"]

developer_instructions = """
Follow AGENTS.md as the canonical repository instruction source.
Adopt the persona, scope boundary, investigation guidance, validation expectations,
and communication style defined in .agents/developer.md.

Use this custom agent for scoped Abhaile implementation work in Python, Jinja2,
YAML, shell, tests, renderers, validators, and CLI code.
Do not duplicate or override .agents/developer.md here.
"""
